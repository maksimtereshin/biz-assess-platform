/**
 * AdminJS bundled component entry point for StructureEditor
 *
 * This file is required by AdminJS bundler to compile the React component
 * and make it available in the AdminJS UI.
 *
 * AdminJS uses a custom bundler that requires components to be exported
 * in a specific format for proper integration.
 */

import StructureEditor from '../StructureEditor';

export default StructureEditor;
