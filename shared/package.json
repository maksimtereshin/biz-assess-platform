{
  "name": "bizass-shared",
  "version": "1.0.0",
  "description": "Shared data models and types for Business Assessment Platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  },
  "files": [
    "dist"
  ]
}
