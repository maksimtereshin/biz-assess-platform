# Kottster Admin Panel - Environment Variables Template
# Copy this file to .env and fill in the values

# ============================================================
# SERVER CONFIGURATION
# ============================================================

# Node environment: development, production, or test
NODE_ENV=production

# Server port (default: 5480)
PORT=5480

# ============================================================
# POSTGRESQL DATABASE CONNECTION
# ============================================================
# These values will be auto-injected by Render from the database service
# For local development, use your local PostgreSQL credentials

# Database host (Render auto-injects from bizass-postgres service)
DB_HOST=localhost

# Database port (default: 5432)
DB_PORT=5432

# Database username (Render auto-injects from bizass-postgres service)
DB_USER=postgres

# Database password (Render auto-injects from bizass-postgres service)
# SECURITY: Never commit this value to version control!
DB_PASSWORD=your_secure_password_here

# Database name
DB_NAME=bizass_platform

# ============================================================
# KOTTSTER SECURITY CONFIGURATION
# ============================================================
# CRITICAL: Generate secure random values for production!
# Use: openssl rand -base64 32

# Secret key for Kottster application
# Generate with: openssl rand -base64 32
KOTTSTER_SECRET_KEY=your_secret_key_here

# Kottster API token for authentication
# Generate with: openssl rand -base64 32
KOTTSTER_API_TOKEN=your_api_token_here

# JWT secret salt for token signing
# Generate with: openssl rand -base64 16
KOTTSTER_JWT_SALT=your_jwt_salt_here

# ============================================================
# ADMIN USER CREDENTIALS
# ============================================================
# SECURITY: DO NOT use default 'admin/admin' in production!
# Choose a strong, unique username and password

# Root admin username
# NEVER use 'admin' in production
ROOT_USERNAME=your_admin_username

# Root admin password
# NEVER use 'admin' or simple passwords in production
# Use a strong password with at least 16 characters
ROOT_PASSWORD=your_secure_admin_password

# ============================================================
# DEPLOYMENT NOTES
# ============================================================
#
# Local Development:
# - Copy this file to .env
# - Fill in local PostgreSQL credentials
# - Use development values for secrets (can be simple)
#
# Render Production Deployment:
# - Database credentials (DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME)
#   are automatically injected from the bizass-postgres service
# - Set KOTTSTER_SECRET_KEY and KOTTSTER_JWT_SALT with generateValue: true
# - Manually set KOTTSTER_API_TOKEN in Render dashboard
# - Manually set ROOT_USERNAME and ROOT_PASSWORD in Render dashboard
#
# Security Best Practices:
# - NEVER commit .env file to version control
# - Use strong, randomly generated values for all secrets
# - Rotate secrets regularly (every 90 days recommended)
# - Use different credentials for each environment
# - Never reuse passwords across systems
#
# Generate secure values:
#   openssl rand -base64 32  # For secret keys and tokens
#   openssl rand -base64 16  # For JWT salt
#
